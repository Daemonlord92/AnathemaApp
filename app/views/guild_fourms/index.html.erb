<p id="notice"><%= notice %></p>

<h1>Guild Fourms</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @guild_fourms.each do |guild_fourm| %>
      <tr>
        <td><%= link_to 'Show', guild_fourm %></td>
        <td><%= link_to 'Edit', edit_guild_fourm_path(guild_fourm) %></td>
        <td><%= link_to 'Destroy', guild_fourm, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Guild Fourm', new_guild_fourm_path %>
